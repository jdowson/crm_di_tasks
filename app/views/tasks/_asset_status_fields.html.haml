%table{ :width => 476, :cellpadding => 0, :cellspacing => 0 }
  %tr
    %td{ :valign => :top }
      .label 
        = I18n.t(:asset_status_create, :scope => [:di, :tasks]) << ":" 
      %b 
        = I18n.t(task.asset_status_create) unless task.asset_status_create.nil? || task.asset_status_create.empty?
    %td= spacer
    %td
      .label 
        = I18n.t(:asset_status_update, :scope => [:di, :tasks]) << ":" 
      %b 
        = I18n.t(task.asset_status_update) unless task.asset_status_update.nil? || task.asset_status_update.empty?
