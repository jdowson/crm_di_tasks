en-US:

  di:

    tasks:
      #----------------------------------------------------------------------------
      # Plugin wide strings.
      #----------------------------------------------------------------------------
      description:           Description
      outcome_type:          Outcome type
      outcome_sub_type:      Subtype
      outcome_text:          Outcome notes
      outcome:               Outcome
      outcome_intro:         Details of how the task was handled, usually only completed once the task is completed.